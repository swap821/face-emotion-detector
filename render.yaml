services:
  - type: web
    name: emotion-detector-api
    env: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt && python train_cnn.py
    startCommand: gunicorn --worker-class eventlet -w 1 app:app